import { PayMoreGetLessValidators } from './pay-more-get-less-validators';

describe('PayMoreGetLessValidators', () => {
  it('should create an instance', () => {
    expect(new PayMoreGetLessValidators()).toBeTruthy();
  });
});
